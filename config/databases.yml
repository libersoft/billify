dev:
  propel:
    param:
      hostspec: localhost
      username: root
      password:
      database: phpaccount

all:
  propel:
    class: sfPropelDatabase
    param:
      datasource:   propel
      phptype:      mysql
      hostspec: localhost
      database: phpaccount
      username: root
      password:
      compat_assoc_lower:  true
      compat_rtrim_string: true
